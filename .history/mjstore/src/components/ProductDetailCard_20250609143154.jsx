import { useState , useEffect } from 'react';
import { Col , Row } from 'react-bootstrap';

const ProductDetailCard = ({ Producct }) => {
    const {length ,width ,height } = Prod
    const { _id, price , title,
            brand, category , color,
            description ,material, name ,
            power, power_supply ,}

}